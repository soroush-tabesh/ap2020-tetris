package ir.soroushtabesh.tetris.views;

import javax.swing.*;
import java.awt.*;

public class MenuPanel extends JPanel {
    // a horizontal fill grid bag for main menu.

    public MenuPanel() {
        super();
        setOpaque(true);
    }

    @Override
    protected void paintComponent(Graphics g) {
//        super.paintComponent(g);
    }
}
